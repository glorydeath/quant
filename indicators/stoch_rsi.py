from indicators.rsi import RSI

# class StochRSI:

    # def get_stoch_rsi(self, data, period):
    #     rsi_func = RSI()
    #     rsi = rsi_func.get_rsi(data, period)
    #     stochrsi = [100 * ((rsi[idx] - np.min(rsi[idx+1-period:idx+1])) / )]
